package com.flamingo.predictandwin.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand Palette ──────────────────────────────────────────────────────────────
val GoldPrimary = Color(0xFFD4A017)        // Rich gold
val GoldLight = Color(0xFFF5D769)          // Soft gold highlight
val GoldDark = Color(0xFFB8860B)           // Dark gold

// ── Surface / Background ───────────────────────────────────────────────────────
val SurfaceDark = Color(0xFF121218)        // Near-black surface
val SurfaceCard = Color(0xFF1C1C24)        // Card background
val SurfaceCardElevated = Color(0xFF25252F) // Elevated card
val SurfaceOverlay = Color(0xFF2A2A36)     // Overlays & sheets

// ── Text ───────────────────────────────────────────────────────────────────────
val TextPrimary = Color(0xFFEDEDF3)
val TextSecondary = Color(0xFF9E9EAF)
val TextMuted = Color(0xFF6B6B7D)

// ── Semantic ───────────────────────────────────────────────────────────────────
val BullishGreen = Color(0xFF00C853)       // Bright green for "UP"
val BullishGreenDark = Color(0xFF1B5E20)
val BearishRed = Color(0xFFFF1744)         // Vivid red for "DOWN"
val BearishRedDark = Color(0xFF7F0000)

// ── Accent / Reward ────────────────────────────────────────────────────────────
val AccentPurple = Color(0xFFBB86FC)
val AccentTeal = Color(0xFF03DAC6)

// ── Milestone tints ────────────────────────────────────────────────────────────
val MilestoneBronze = Color(0xFFCD7F32)
val MilestoneSilver = Color(0xFFC0C0C0)
val MilestoneGold = Color(0xFFFFD700)
